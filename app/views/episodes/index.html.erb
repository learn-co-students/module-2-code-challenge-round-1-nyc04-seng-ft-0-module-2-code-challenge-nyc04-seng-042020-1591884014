<h1>All Episodes</h1>

<ul>
  <% @episodes.each do |episode| %>
    <li><%= "Episode #{episode.number} - #{episode.date.strftime("%B %d, %Y")}"%> <%= link_to 'show page', episode_path(episode) %></li>
  
  <% end %>
</ul>
